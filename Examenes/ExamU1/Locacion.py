class Locacion:
    def __init__(self,nombreLocacion,porcentajeIVA) -> None:
        self._nombreLocacion = nombreLocacion #Nombre de la locación
        self._porcentajeIVA = porcentajeIVA #Valor flotante
        self.listaItems = [] #Lista de Items

        #Método para agregar elementos a listaItems
        def agregarItems(self, elemento):
            self.listaItems.append(elemento)
        